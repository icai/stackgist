// import DevServer from './lib/dev_server';

export default agent => startDevServer(agent);

function startDevServer(_agent) {
//   const server = new DevServer(agent);
//   server.ready(err => {
//     if (err) agent.coreLogger.error('[egg-view-nextjs]', err.message);
//   });

//   agent.beforeStart(async () => {
//     await server.ready();
//   });
//   agent.beforeClose(async () => {
//     await server.close();
//   });
}
